# SlateIT_Node

To install and run:

1) Download and install Node and npm

2) Checkout git repo

3) Navigate to git directory and `npm install`

4) Download and install MongoDB

5) Start MongoDB service with `mongod --dbpath={PATH_TO_REPO}/mongoData/`

6) Run the code with `node bin/www`
